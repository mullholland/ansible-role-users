---
# This file is used by ansible-generator.

# CentOS isn't a valid Galaxy platform name (only "EL" is), but the role
# has no distro-specific logic (plain ansible.builtin.user/group), so it's
# added to the CI matrix and README compatibility table here instead.
extra_platforms:
  - name: CentOS
    versions:
      - all
